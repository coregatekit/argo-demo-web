apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: argodemo-web
spec:
  rules:
    - host: argodemo-web-dev.coregate.dev
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: argodemo-web
                port:
                  number: 3000
